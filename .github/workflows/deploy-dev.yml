name: Deploy Dev

# CI gate on every push to `dev`.
# Vercel deploys automatically via its GitHub integration.
# Firebase is deployed manually.

on:
  push:
    branches: [dev]
  workflow_dispatch:

jobs:
  gate:
    uses: ./.github/workflows/ci.yml
